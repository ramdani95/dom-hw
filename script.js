let quotes = [
  `I live my life a quarter mile at a time`,
  `I said a ten-second car, not a ten-minute car`,
  `You can have any brew you want... as long as it's a Corona.`,
  `You almost had me? You never had me - you never had your car!`,
  `I don't have friends. I have family.`,
  `It don't matter if you win by an inch or a mile. Winning's winning.`,
];

window.onload =  function(event) {
  
  // Random quote of the day generator
  /*const randomQuote = function() {
    document.querySelector('#quote-of-the-day').textContent = `"${
      quotes[Math.floor(Math.random() * quotes.length)]
    }"`;
  };
  randomQuote();

  // Do all of your work inside the window.onload function (in other words, here!)

  // Part 1*/
 document.getElementById(`main-title`).textContent= (`Hi! i am DOM Toretto`)
  // Part 2
 var bodyStyle= document.querySelector(`body`)
  bodyStyle.style.backgroundColor= `pink`  
  // Part 3
const itemList = document.getElementById(`favorite-things`);
itemList = remove.li.child [5]

  // Part 4
var div= document.getElementById(`quote-title`) 
specialTitle.fontSize ="2rem"  
var divone= document.getElementById(`dom-adventures`) 
specialTitle.style.fontSize ="2rem"  
 

  // Part 5
  const list = document.getElementById("past-races");

  if (list.hasChildNodes(ul)) {
    list.removeChild(list.children[3]);
  }
  // Part 6

  // Part 7
}